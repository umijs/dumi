// console.log(getAllRoutesMeta());
// console.log(getRouteMetaById(xx)); // xx 可以从 `.dumi/tmp/core/route.ts` 里找一个 Markdown 路由的 id
// console.log(getDemoById('foo-demo-0'));

console.log('>>>');

export default () => '自定义 React 页面测试';
