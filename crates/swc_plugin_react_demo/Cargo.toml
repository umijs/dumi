[package]
name = "swc_plugin_react_demo"
version = "0.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
swc_core = { version = "6.0.1", features = [
  "ecma_plugin_transform",
  "ecma_visit",
  "ecma_ast",
  "common"
]}
